{
  "name": "napoli-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "npx prettier --check . && mocha",
    "postinstall": "node bin/npm-postinstall.js"
  },
  "devDependencies": {
    "mocha": "^6.0.0",
    "node-gyp": "^9.3.0",
    "prettier": "2.7.1"
  }
}
